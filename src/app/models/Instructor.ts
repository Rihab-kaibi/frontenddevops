 export interface Instructor {
    numInstructor: number; // ID of the instructor
    firstName: string;     // First name of the instructor
    lastName: string;      // Last name of the instructor
    dateOfHire: string;    // Date of hire (can be string or Date, depending on your usage)
}
